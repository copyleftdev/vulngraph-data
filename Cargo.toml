[workspace]
members = ["crates/vulngraph-data"]
resolver = "2"

# Local development override: uncomment to iterate against an uncommitted
# engine checkout instead of the pinned git tag. Do not commit it enabled.
# [patch."https://github.com/copyleftdev/vulngraph"]
# vulngraph-engine = { path = "../vulngraph/engine" }
